<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quangminh.myiotproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.DangNhapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.DangKyActivity" />
        <activity android:name=".Activity.TrangChuActivity" />
        <activity android:name=".Activity.ThemThietBiActivity"
            android:theme="@style/PopUpActivity"
            />
        <activity android:name=".Activity.RoomActivity" />
        <activity android:name=".Activity.DeviceActivity" />
        <activity android:name=".Activity.ActivityControlTV" />
    </application>

</manifest>